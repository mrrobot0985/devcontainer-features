{
  "id": "mkdocs-material",
  "name": "MkDocs Material",
  "version": "1.0.0",
  "description": "Installs MkDocs with Material theme and popular plugins for documentation sites in devcontainers",
  "options": {
    "plugins": {
      "type": "string",
      "default": "search,minify,git-revision-date,redirects",
      "description": "Comma-separated list of MkDocs Material plugins to install"
    },
    "generateConfig": {
      "type": "boolean",
      "default": true,
      "description": "Generate a default mkdocs.yml if one doesn't exist in the workspace"
    },
    "servePort": {
      "type": "string",
      "default": "8000",
      "description": "Default port for mkdocs serve"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ],
  "documentationURL": "https://github.com/mrrobot0985/devcontainer-features/tree/main/src/mkdocs-material"
}
