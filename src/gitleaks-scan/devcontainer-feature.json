{
  "id": "gitleaks-scan",
  "name": "Gitleaks Secret Scanner",
  "version": "1.0.0",
  "description": "Installs Gitleaks for detecting and preventing hardcoded secrets in git repositories",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version of gitleaks to install, or 'latest'"
    },
    "installPreCommitHook": {
      "type": "boolean",
      "default": false,
      "description": "Install gitleaks as a pre-commit hook in the workspace repository"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/git",
    "ghcr.io/devcontainers/features/common-utils"
  ],
  "documentationURL": "https://github.com/mrrobot0985/devcontainer-features/tree/main/src/gitleaks-scan"
}
