{
  "id": "podman-checkpoint-helper",
  "name": "Podman Checkpoint Helper",
  "version": "1.0.0",
  "description": "Installs Podman with checkpoint/restore support (CRIU) for ephemeral AI agent container workflows and cross-node container migration",
  "options": {
    "installCriu": {
      "type": "boolean",
      "default": true,
      "description": "Install CRIU (Checkpoint/Restore In Userspace) required for Podman checkpoint/restore"
    },
    "configureStorage": {
      "type": "boolean",
      "default": true,
      "description": "Configure Podman storage to use overlay filesystem for checkpoint compatibility"
    },
    "addAliases": {
      "type": "boolean",
      "default": true,
      "description": "Add docker-compatible aliases (docker, docker-compose) that wrap Podman commands"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ],
  "documentationURL": "https://github.com/mrrobot0985/devcontainer-features/tree/main/src/podman-checkpoint-helper",
  "licenseURI": "https://github.com/mrrobot0985/devcontainer-features/blob/main/LICENSE"
}
