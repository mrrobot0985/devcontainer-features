{
  "id": "host-isolation",
  "name": "Host Isolation Security Profile",
  "version": "0.1.0",
  "description": "Audits devcontainer.json for unsafe runArgs, mounts, and capabilities. Warns when privileged mode, Docker socket binds, or excessive capabilities are detected.",
  "options": {
    "failOnWarning": {
      "type": "boolean",
      "default": false,
      "description": "Fail container creation if unsafe configurations are detected"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
