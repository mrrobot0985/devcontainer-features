{
  "id": "container-firewall",
  "name": "Container Firewall",
  "version": "0.4.0",
  "description": "Configures an iptables/ipset whitelist firewall for the container with selectable service tags and optional telemetry blocking",
  "options": {
    "services": {
      "type": "string",
      "proposals": [
        "minimal",
        "claude-code",
        "github",
        "npm",
        "pypi",
        "apt",
        "docker",
        "vscode",
        "astral",
        "anthropic",
        "huggingface",
        "gitlab",
        "openrouter",
        "google"
      ],
      "default": "claude-code",
      "description": "Comma-separated service tags to whitelist. Use 'minimal' for an empty baseline. Composite tags like 'claude-code' expand to multiple services."
    },
    "extraDomains": {
      "type": "string",
      "default": "",
      "description": "Additional comma-separated domains to whitelist beyond the selected services."
    },
    "blockTelemetry": {
      "type": "boolean",
      "default": false,
      "description": "Block known telemetry and tracking endpoints at the network level"
    },
    "policy": {
      "type": "string",
      "enum": [
        "whitelist",
        "monitor"
      ],
      "default": "whitelist",
      "description": "whitelist drops non-matching traffic; monitor logs but does not block"
    },
    "enableIPv6": {
      "type": "boolean",
      "default": true,
      "description": "Also apply whitelist rules to IPv6 (ip6tables)"
    },
    "failIfUnprivileged": {
      "type": "boolean",
      "default": true,
      "description": "Fail container creation when iptables cannot be used (missing CAP_NET_ADMIN). When false, emit a warning and continue as a no-op."
    },
    "dryRun": {
      "type": "boolean",
      "default": false,
      "description": "Resolve all configured domains and print the IPs/CIDRs and policy that would be applied without modifying iptables or ipset rules."
    }
  },
  "capAdd": [
    "NET_ADMIN"
  ],
  "postCreateCommand": "sudo /usr/local/bin/container-firewall-init",
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
