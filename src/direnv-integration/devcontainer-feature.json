{
  "id": "direnv-integration",
  "name": "direnv Integration",
  "version": "1.0.0",
  "description": "Installs direnv via apt-get and hooks it into shell startup for automatic .envrc loading when changing directories inside the devcontainer",
  "options": {
    "shell": {
      "type": "string",
      "default": "auto",
      "description": "Shells to hook into: auto, bash, zsh, fish. Use comma-separated for multiple.",
      "enum": [
        "auto",
        "bash",
        "zsh",
        "fish"
      ]
    },
    "autoAllow": {
      "type": "boolean",
      "default": true,
      "description": "Automatically run 'direnv allow' in workspace directories where .envrc exists"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ],
  "documentationURL": "https://github.com/mrrobot0985/devcontainer-features/tree/main/src/direnv-integration",
  "licenseURI": "https://github.com/mrrobot0985/devcontainer-features/blob/main/LICENSE"
}
