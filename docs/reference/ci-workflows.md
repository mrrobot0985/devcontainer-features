# CI Workflows

All CI runs on every push to `main` and on every pull request. Workflows are split so each check has a single responsibility. The checks listed below are required to pass before a PR can merge.

Release publishing is separate: it runs only when a developer pushes a prefixed tag (or manually dispatches the release workflow for backfill).

## Workflow summary

| Workflow                                     | File                                         | Trigger                       | What it checks / does                                                                                                                                   |
| -------------------------------------------- | -------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **CI - Test Features**                       | `.github/workflows/test.yaml`                | push to `main`, PR, manual    | Builds each feature in autogenerated default tests, per-feature scenario tests, and global integration scenarios. Fails if any required test job fails. |
| **Validate devcontainer-feature.json files** | `.github/workflows/validate.yml`             | push to `main`, PR, manual    | Validates JSON metadata schema, runs shellcheck on scripts, and checks that feature READMEs are in sync with JSON metadata.                             |
| **Lint workflows**                           | `.github/workflows/lint-workflows.yml`       | push to `main`, PR, manual    | Lints all GitHub Actions workflow files with `actionlint`.                                                                                              |
| **Conventional Commits**                     | `.github/workflows/conventional-commits.yml` | PR open/edit/sync             | Ensures the PR title follows Conventional Commits.                                                                                                      |
| **Release dev container features**           | `.github/workflows/release.yaml`             | push of `*-v*` tag, or manual | Publishes features under `./src` to GitHub Container Registry. Does not create tags.                                                                    |

## CI - Test Features

File: `.github/workflows/test.yaml`

This is the primary test gate. Job count is kept intentional so PRs do not spawn a full monorepo matrix:

- `setup` — selects which features to test:
  - Always **excludes** ids listed in `.github/ci-skip-features.txt` (pure installers better served by official/community features; see monorepo NOTES / feature-catalog policy).
  - **Pull request:** only remaining features with paths changed under `src/<id>/` or `test/<id>/` (full CI-owned set if `test.yaml`, `ci-skip-features.txt`, or `scripts/` change).
  - **main / manual:** every CI-owned feature under `src/`.
- `test-autogenerated` — default-install test on **one** base image (`mcr.microsoft.com/devcontainers/base:ubuntu`). A few network-heavy CI-owned features are also matrix-excluded and covered via scenarios where defined.
- `test-scenarios` — only for selected features that have `test/<feature>/scenarios.json`.
- `test-global` — cross-feature integration scenarios from `test/_global/scenarios.json` (always one job).
- `test-summary` — fails the workflow if any required test job failed; skipped matrices (e.g. docs-only PRs) are allowed.

Skipped features remain in the repo for existing consumers but are not exercised in CI. Prefer official or community features for those installers when starting new projects.

## Validate devcontainer-feature.json files

File: `.github/workflows/validate.yml`

Parallel jobs:

- `validate` — uses `devcontainers/action@v1` with `validate-only: "true"` to validate every `devcontainer-feature.json` against the devcontainer features schema.
- `shellcheck` — runs `shellcheck --severity=warning` on `scripts/local-ci.sh`, `.githooks/pre-commit`, `src/*/install.sh`, `src/*/uninstall.sh`, and all test scripts.
- `readme-sync` — runs `uv run python scripts/generate-feature-readmes.py --check` to ensure each feature README matches its JSON metadata.
- `docs-check` — runs `mdformat --check` on documentation and selected GitHub markdown files.

## Lint workflows

File: `.github/workflows/lint-workflows.yml`

Installs `actionlint` and runs it against every workflow file in `.github/workflows/`. Catches syntax errors, missing permissions, insecure expressions, and Actions-specific issues.

## Conventional Commits

File: `.github/workflows/conventional-commits.yml`

Runs `amannn/action-semantic-pull-request@v5` on PR open, edit, and synchronize events. Validates the PR title against the Conventional Commits types used in this repository:

`feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`, `ci`, `build`, `perf`.

Because squash merges use the PR title as the commit message, this keeps `main` branch history consistent.

## Release dev container features

File: `.github/workflows/release.yaml`

Triggered by prefixed tags (`*-v*`) that a developer creates and pushes, or by manual `workflow_dispatch` for backfill. Publishes features under `./src` to GitHub Container Registry using `devcontainers/action@v1` with `publish-features: "true"`.

This workflow never creates git tags. Version bumps and tags are done in normal development git work. See [Releasing a Feature](../how-to-guides/release-a-feature.md).

## Required status checks

The following workflows are required to pass before a PR merges:

- `test.yaml`
- `validate.yml`
- `lint-workflows.yml`
- `conventional-commits.yml`

## See also

- [Scripts and Automation](scripts.md)
- [Releasing a Feature](../how-to-guides/release-a-feature.md)
